# delegate-doctor

Diagnostique la configuration du plugin claude-delegate : détecte le backend actif (Ollama), liste les modèles disponibles, et affiche le routing sélectionné pour une requête de test.

## Exécution

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/scripts/router.py" "test"
```
